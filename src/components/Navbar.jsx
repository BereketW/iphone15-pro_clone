import {appleImg} from "../utils/index.js";

export const Navbar = () => {
    return (
        <header>
            <nav>
                <img src={appleImg} alt="logo" />
            </nav>
        </header>
    )
}
